# Suggestion Session Configuration
# Set to a fixed value (e.g., "suggestions-shared") to use the same session ID for all suggestion requests
# Leave empty or omit to generate unique session IDs (suggestion-{timestamp})
VITE_SUGGESTION_SESSION_ID=

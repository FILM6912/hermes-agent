# Hermes Web UI — Python dependencies (WebUI + Document RAG API + ASR)
# Install: uv pip install -r requirements.txt

# --- WebUI core ---
pyyaml>=6.0
cryptography>=42.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
# Optional for HERMES_WEBUI_DATABASE_URL=postgresql://... (Supabase). SQLite needs no extra driver.
psycopg[binary]>=3.2.0
hindsight-client>=0.4.22

# Hugging Face local embedding/reranker (EMBEDDING_BACKEND=huggingface)
sentence-transformers>=3.4.0
torch
torchvision
accelerate
# Required when EMBEDDING_LOAD_BITS=4|8 or RERANKER_LOAD_BITS=4|8
bitsandbytes>=0.43.0

# --- ASR (ASR_ENABLED=true) — HTTP OpenAI-compatible /v1/audio/transcriptions ---
